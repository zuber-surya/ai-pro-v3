/**
 * Seed placeholder — expand per docs/04_DATABASE_DESIGN_DOCUMENT.md §9.
 * Run: npm run prisma:seed (non-production only).
 */
console.log("Seed stub: no demo rows yet. Implement after schema expansion.");
