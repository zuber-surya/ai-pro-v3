"use client";

import { CommandCenterPanel } from "@/features/admin/commandCenter";

/** SCR-CMD — Admin / Agent command center */
export default function AdminHomePage() {
  return <CommandCenterPanel />;
}
